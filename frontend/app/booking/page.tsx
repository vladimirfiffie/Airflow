export default function BookingPage() {
  return (
    <div className="rounded-2xl border border-slate-200 bg-white p-8 shadow-sm">
      <h1 className="text-3xl font-black tracking-tight text-slate-900">Manage Booking</h1>
      <p className="mt-2 text-slate-600">
        This page is intentionally frontend-only for now. Hook booking references and traveler records to backend APIs
        when ready.
      </p>
    </div>
  );
}
